# 2026-06-14 对话全文

## 1. 全产品复盘审计（17:04）
Jarry：全产品复盘审计
→ V1 用当时模型做全量扫描，发现 6 个后端孤儿 API、前端 5 个 tab 中 2 个纯占位（profile/calendar）、onboarding 假存储
→ Jarry 要求切 v4-pro 后重审
→ V2 用 v4-pro 重审：更严谨的代码/数据/流程缺陷分析

## 2. 飞书渲染 Bug 排查（约 17:30-17:40）
Jarry：除了问号什么都不回复
→ 排查发现正常回复通道到飞书渲染失败，message 工具直接发送正常
→ 根因：自动回复通道对复杂格式额外转义
→ Jarry 要求修复

## 3. 菜谱推荐改造（约 17:45-19:00）
Jarry：现在菜谱推荐应该用 DeepSeek v4-pro，之前不是说用这个吗
→ 把 recipes.py 从硬编码 mock 改为调 DeepSeek v4-pro 实时生成
→ 部署后 v4-pro 首次调用 JSON 截断，修复 max_tokens 6000 + 容错
→ Jarry 反馈：没看出 v4-pro 质量高在哪
→ 排查发现缓存里一直是 fallback 数据，v4-pro 没跑成功
→ 修复 systemd Environment 注入 DEEPSEEK_API_KEY
→ v4-pro 成功生成，耗时 ~57s，质量明显优于 chat

## 4. 菜谱改成早中晚三段（约 19:00-19:10）
Jarry：分早中晚，早餐2道、午餐2道、晚餐2道
→ 改 RECIPE_SYSTEM_PROMPT，返回格式从数组改为 {"breakfast":[],"lunch":[],"dinner":[]}
→ 同日 Jarry 说每天预生成 9 道、一次展示 3 道，后来改为 6 道（早2+午2+晚2）

## 5. A/B 对比 deepseek-chat vs v4-pro（约 19:10）
Jarry：对比 chat 和 v4-pro 质量差多少，没大差别就直接用 chat
→ chat（5.4s）：缺 emoji/时间/难度/营养，把盐油当食材列，菜名平淡
→ v4-pro（56.8s）：结构完整，菜名有辨识度，有创意组合，烹饪有技法
→ Jarry 结论：保留 v4-pro，用预生成缓存

## 6. 缓存预生成方案落实（约 19:10-19:20）
Jarry：维持 v4-pro，做预生成
→ recipe_cache 表存三餐 JSON，generated_at 检查跨天
→ GET /api/recipes 秒读缓存 + 跨天自动后台刷新
→ POST /api/recipes/refresh 强制刷新
→ 库存变动（addInventory）后台异步刷新
→ recipe_refresh_log 记录每次生成耗时

## 7. 前端菜谱页面适配（约 19:20-19:25）
Jarry：前端是不是还没配置好？网页显示"暂无推荐"
→ api.ts：getRecipes 返回类型从 any[] 改为 DailyRecipes 对象，新增 RecipeItem 接口，导出 BASE_URL
→ useLiveData.ts：useRecipes hook 返回 { meals, recipes }，recipes 为平铺数组兼容首页
→ recipes.tsx：重写为早中晚三段式，换一批接 POST refresh，点展开看步骤/营养/tip/需采购
→ expo export + 部署到腾讯云

## 8. 纪律性复盘（约 19:26）
Jarry：复盘 SKILL 纪律性问题，分析原因，把之前缺的都补上
→ 根因分析输出
→ 批量修复开始
